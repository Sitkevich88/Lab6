/*
package commands.unknownUsage;

import commands.AbstractCommandWhichRequiresCollection;
import data.MusicBand;
import utils.MessagesForClient;

import java.util.concurrent.LinkedBlockingQueue;

*/
/**
 * Command 'show_all'. Prints all objects of the collection
 *//*


public class ShowAll extends AbstractCommandWhichRequiresCollection {


    public ShowAll(LinkedBlockingQueue<MusicBand> collection, MessagesForClient messages) {
        super(collection, messages);
    }

    public void invoke(){
        getMessages().recordMessage(getCollection().toString());
    }
}
*/
